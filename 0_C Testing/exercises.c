#include <stdio.h>
#include <math.h>

int main(){
	/*
	https://www.w3resource.com/c-programming-exercises/basic-declarations-and-expressions/index.php
	*//*
	Exercise #4 - Print chars forwards and backwards:
	char a, b, c;
	a = 'X';
	b = 'M';
	c = 'L';
	printf("Forwards: %c%c%c\n", a, b, c);
	printf("Backwards: %c%c%c\n", c, b, a);*/

	/*
	Exercise #5 - perimeter and area of a rectangle:
	int l, w, p, a;
	printf("Enter the length: ");
	scanf("%d", &l);
	printf("Enter the width: ");
	scanf("%d", &w);
	p = (2 * l) + (2 * w);
	a = l * w;
	printf("Perimeter: %d\nArea: %d\n", p, a);*/

	/*
	Exercise #6 - Area and Circumference of a Circle:
	int r, a, c;
	printf("Enter the radius: ");
	scanf("%d", &r);
	a = M_PI * (r * r);
	c = 2 * M_PI * r;
	printf("Area: %d\nCircumference: %d\n", a, c);*/

	/*
	Exercise #7 - Random Math Printing:
	int a = 125, b = 12345;
	long ax = 1234567890;
	short s = 4043;
	float x = 2.13459;
	double dx = 1.1415927;
	char c = 'W';
	unsigned long ux = 2541567890;

	printf("%d, %f, %ld, %f, %d, %ld, %d, %ld, %ld\n", a + c, x + c, ((int) dx) + ax, a + x, s + b, ax + b, s + c, ax + c, ax + ux);*/
}